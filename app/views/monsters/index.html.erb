<%# Update me %>
<% @monsters.each do |monster| %>
    <p class="sin_salto">Nombre: </p>
    <% if Tweet.where(monster_id: monster.id).size >0 %>
        <%= link_to monster.name, monster %>
        <% if Tweet.where(monster_id: monster.id).size >1 %>
            <br>
            <p class="red">Tuitero Destacado</p>
        <% end %>
    <% else %>
        <%= link_to monster.name, new_tweet_path %>
    <% end %>
    <br>
    <br>
<% end %>